# Multi-Agent 智能化升级蓝图

## 文档定位

本文档是本项目后续智能化升级改造的最高层纲领性文件，用于统一长期目标、架构方向、产品原则与阶段演进路线。

- 面向对象以工程实施为主，同时兼顾导师沟通、答辩展示与阶段汇报可读性。
- 本文档关注“为什么这样演进”和“系统最终要变成什么样”，不是逐项实现说明书，也不是营销文案。
- 当具体实现方案、阶段排期或局部设计发生变化时，应以本文档定义的方向性原则为总约束。

## 北极星目标

本项目的最终产品目标是：

> 用户只需选择文件，然后静候佳音；系统自动完成数据理解、异常扫描、修复规划、修复执行、结果验证、回滚保护与最终解释。

围绕这一目标，后续升级必须持续提升以下能力：

- 更低的用户决策负担：把复杂参数、策略选择、流程判断尽量收敛到系统内部。
- 更高的一次通过率：尽量让首轮扫描与修复就能得到可信、可用的结果。
- 更强的可解释性与信任感：让用户明确知道系统发现了什么、改了什么、为什么这样改。
- 更强的可靠性与可回滚性：自动化越强，验证、审计和回滚机制越要强。
- 更丰富的结果表达能力：最终结果不能只停留在表格和 JSON，必须能够通过图表帮助用户理解异常与修复效果。

多 agent 只是达到上述目标的手段，而不是目标本身。任何“为了更像 AI 而增加 agent”的设计，都不应成为本项目的默认方向。

## 当前必须保留的项目资产

本项目已经形成了一批高价值资产，后续升级应以复用和包装为主，而不是推倒重来。

### 双轨产品现实

- `app.py` 继续保留为算法验证、结果对照、教学演示和答辩展示路径。
- `appshell/` 继续作为产品化主路径，承担桌面端交付与长期演进职责。

### 现有 Python 引擎能力

当前 Python 引擎已经形成稳定动作边界，应作为未来 Tool Layer 的基础：

- `health`
- `train`
- `scan_file`
- `repair`
- `repair_batch`
- `rollback_repair_batch`

这些动作已经通过 JSON 协议稳定暴露，不应因引入 multi-agent 而被轻易打破。

### 现有算法价值

现有算法并不过时，反而是未来多 agent 系统中最重要的“确定性工具层”。

- `LightGBM` 继续承担训练、风险打分、阈值控制、特征重要性和候选方案排序职责。
- 基于规则的异常扫描继续承担缺失值、离群点、稀有类别、跨列一致性、重复记录、时序偏移等检测职责。
- `src/repair_core.py` 继续作为当前主修复引擎，负责可审计、可验证的确定性修复路径。
- `src/repair_module.py` 中的 Gower 距离与 KNN 邻居检索仍具有混合类型数据修复价值，应在后续被重新包装为专门工具，而不是被废弃。

### 现有工程资产

- Go 侧任务编排、超时取消、任务历史、结构化日志和启动自检已经具备良好基础。
- Wails 前端已经具备扫描、修复、历史查看和可观测性展示能力。
- 当前结果目录、SQLite 历史库和回滚机制已经形成可延伸的产品骨架。

## 纲领性原则

### 1. 复用优先，重写从严

优先复用现有 Python 引擎、Go 编排层、Wails 前端、LightGBM、规则扫描和 Gower 距离资产。只有在原有资产无法满足目标时，才考虑重写。

### 2. 工具在下，Agent 在上

系统应明确分层：

- 下层是确定性的工具与算法执行层，负责扫描、打分、修复、验证、回滚。
- 上层是 agent 决策层，负责理解用户意图、选择工具、组合策略、解释结果、管理记忆。

agent 不直接替代强算法，而是编排和增强强算法。

### 3. 执行真相唯一

同一类扫描、修复、验证逻辑必须有唯一执行来源。不能让多个 agent 各自实现一套扫描逻辑，造成结果不一致。

### 4. 自动化增强必须伴随安全增强

本项目的默认长期目标是“全自动闭环”，但前提是：

- 自动执行前有明确验证门槛。
- 自动执行后有清晰结果比对。
- 任意高风险修复都有可追踪回滚路径。

### 5. 模型与厂商解耦

未来大模型能力应通过 Agent Runtime 与 Tool Layer 接入，保持供应商可替换、策略可升级、调用可追踪，不应过早绑定单一模型平台。

### 6. 可观测、可回放、可审计

多 agent 流程必须天然具备以下属性：

- 能看到每一步做了什么决定。
- 能看到调用了哪些工具。
- 能看到为什么接受或拒绝某个修复计划。
- 能在事后复盘整次任务。

### 7. 新手极简，专家可控

- 默认模式追求“选文件即可运行”。
- 高级模式保留策略、轨迹、解释、审批和人工覆盖入口。

## 目标架构

本项目未来的目标架构分为四层：

### 1. User Experience Layer

面向最终用户提供统一的桌面端交互体验，包括：

- 文件选择与任务发起
- 自动化结果展示
- 图表化异常解释
- 修复结果与回滚入口
- agent 轨迹与审计信息查看

### 2. Go Orchestration / Agent Runtime Layer

以 Go 为核心承载任务生命周期与 agent 协作运行时，包括：

- 会话管理
- agent 编排
- 工具调用调度
- 超时、取消、预算与重试控制
- 任务历史与轨迹持久化
- 审批与验证闸门

### 3. Deterministic Tool Layer

在现有 Python 引擎动作基础上形成统一工具注册层，包括：

- 扫描工具
- 训练工具
- 单样本修复工具
- 批量修复工具
- 回滚工具
- LightGBM 打分工具
- Gower 邻居检索工具

这一层的职责不是“理解用户”，而是稳定、可重复地完成执行。

### 4. Algorithm & Artifact Layer

承载现有算法、模型、规则与产物，包括：

- `LightGBM` 训练与评分结果
- 基于规则的异常扫描逻辑
- `repair_core.py` 的确定性修复逻辑
- `repair_module.py` 的 Gower 距离邻居检索逻辑
- 模型文件、修复清单、回滚清单、任务历史与图表数据产物

### 架构叙事

未来系统应形成“前端发起任务，Go 运行时负责多 agent 编排，agent 选择并组合现有 Python 工具，工具继续调用现有算法资产，最后由系统输出修复结果、回滚凭据和可解释图表”的稳定闭环。

## 12-Agent 协作蓝图

推荐采用以下 12 个角色作为长期目标蓝图：

1. `Supervisor Agent`
2. `Intent Agent`
3. `Profile Agent`
4. `Strategy Agent`
5. `Missing Agent`
6. `Outlier Agent`
7. `Rare Agent`
8. `Consistency Agent`
9. `Retrieval Agent`
10. `Scoring Agent`
11. `Repair Planner Agent`
12. `Validator Agent`

### 角色定位

- `Supervisor Agent` 负责全局编排、预算控制、结果汇总和任务收敛。
- `Intent Agent` 负责把用户需求翻译为结构化目标。
- `Profile Agent` 负责理解数据结构、字段类型、候选主键和基础画像。
- `Strategy Agent` 负责选择扫描参数、修复路径和执行策略。
- `Missing / Outlier / Rare / Consistency Agent` 分别聚焦对应问题类型的建议生成与策略判断。
- `Retrieval Agent` 负责调用基于 Gower 的健康邻居检索能力，生成混合类型场景下的候选修复值。
- `Scoring Agent` 负责调用基于 LightGBM 的评分与候选排序能力。
- `Repair Planner Agent` 负责将规则、邻居候选、评分结果整合为可执行修复计划。
- `Validator Agent` 负责修复前后比对、效果验证、接受或回滚建议。

### 激活策略

- 不是每次会话都必须启用全部 12 个 agent。
- 激活应当按需发生，遵循问题驱动，而不是常驻群聊式协作。
- 没有对应问题时，不应强行唤起对应专家 agent。

### 权限原则

- agent 负责判断和规划。
- 写入文件、落地修复、执行回滚等高影响动作，只能由受控执行工具完成。
- 任意 agent 都不应绕开 Tool Layer 直接修改结果文件。

## 重要接口与约束

后续实现必须遵守以下概念边界。

### Agent Runtime

- 输入：任务上下文、用户约束、扫描结果、历史记录、工具返回结果。
- 输出：结构化决策、下一步工具调用、最终计划或最终解释。

### Tool Registry

- 负责包装现有 Python engine actions。
- 负责将未来新增能力以“适配器”的形式加入，而不是破坏原有动作边界。
- 未来新增工具应优先采用 `repair_with_gower` 这类包装式命名，而不是直接重写主修复逻辑。

### Task Trace

- 必须记录 agent 步骤、工具调用、关键决策、审批信息、产物路径和最终状态。
- 必须可持久化到现有任务历史体系中，避免智能化后出现“结果有了但过程丢失”的黑盒问题。

### Validation Gate

- 负责决定某一修复计划是执行、拒绝还是回滚。
- 必须在全自动闭环中承担质量与安全兜底职责。

### 协议方向

- 请求/响应仍以 JSON 为主。
- 现有 backend 约定应继续保持兼容。
- Tool Layer 可以扩展，但不应随意打破已有动作接口与任务语义。

## 目标用户体验

默认目标体验应当是：

1. 选择文件
2. 系统理解数据
3. 系统扫描异常
4. 系统选择修复策略
5. 系统验证修复效果
6. 系统输出修复结果、摘要解释与回滚参考

### 基础模式

- 用户尽量不需要理解参数含义。
- 系统自动选择默认策略并执行闭环。
- 输出结果应以摘要和图表优先，而不是原始结构化数据优先。

### 高级模式

- 可查看 agent 轨迹与工具调用。
- 可查看风险判断、策略理由、被跳过的问题和验证细节。
- 可人工覆盖部分策略，或对高风险步骤进行审批。

### 信任建设要求

系统必须始终回答清楚以下问题：

- 发现了什么异常？
- 为什么判断它是异常？
- 修改了哪些值？
- 为什么这样修改？
- 修改后风险降低了多少？
- 如果不满意，怎样回滚？

## 可视化愿景

最终系统必须以图表作为理解入口，而不是只输出表格和 JSON。

### 一、异常理解类图表

- 列风险排序图
- 异常密度热力图
- 问题类型分布图
- 高风险列聚焦图
- 关键字段异常缩略图

### 二、修复影响类图表

- 修复前后问题数量对比图
- 已修复与已跳过问题构成图
- 风险分数变化分布图
- 置信度变化图
- 单列修复收益对比图

### 三、执行与可观测类图表

- 任务进度时间线
- agent 决策轨迹图
- 工具调用链路图
- 验证与回滚摘要图

### 继承并扩展当前优势

未来图表系统必须明确继承并升级现有可视化基础：

- 来自 Streamlit 路径的 `ROC / 混淆矩阵 / 特征重要性 / SHAP 解释`
- 来自 Wails 路径的 `issue map / 热点缩略图 / 前后对比条 / 任务时间线 / observability`

### 图表原则

图表存在的目的，是帮助用户理解异常与修复结果，而不是为了让界面显得“更智能”或“更炫”。

## 长期路线图

### Stage 0：保留与包装现有资产

目标是先明确哪些能力必须保留，哪些能力应该被包装进未来工具层。
Stage 0 的具体落地定义、术语和 action/tool 映射见 `TOOL_LAYER_FOUNDATION.md`。

阶段结果：

- 现有 Python engine actions 继续稳定可用。
- 现有算法资产被正式定义为未来多 agent 系统的工具基础。
- 项目形成统一的长期升级蓝图和术语体系。

### Stage 1：在现有工具外增加 Agent Runtime

目标是在不破坏现有执行链路的前提下，将 agent 引入编排层。

阶段结果：

- 系统可以基于会话上下文调度 agent。
- agent 能围绕现有扫描和修复工具生成计划与解释。
- 任务历史开始沉淀 agent 轨迹。

### Stage 2：将 Gower 能力重新引入为专门工具

目标是让混合类型近邻检索成为正式能力，而不是遗留模块。

阶段结果：

- Gower 邻居检索被包装为独立工具。
- 系统可以在规则修复与邻居修复之间进行选择或组合。
- 修复候选来源从单一路径提升为多路径协同。

### Stage 3：形成验证优先的全自动闭环

目标是让系统在“自动化增强”的同时完成“安全增强”。

阶段结果：

- 系统具备自动修复、自动复扫、自动验证和自动回滚建议能力。
- 高风险动作不再依赖人工兜底才能保证安全。
- 全自动模式具备可控可审计基础。

### Stage 4：升级解释系统与图表系统

目标是让结果真正可理解、可汇报、可展示。

阶段结果：

- 形成系统级图表目录和统一解释模板。
- 用户能快速理解异常分布、修复收益和风险变化。
- 工程端与答辩端共享同一套高质量表达资产。

### Stage 5：打磨“一键上传，静候结果”的产品体验

目标是将智能化能力最终收敛为低门槛、高可信的默认体验。

阶段结果：

- 新手用户无需理解复杂参数即可获得高质量结果。
- 高级用户仍然可以查看轨迹、理由与审计信息。
- 产品体验从“工具可用”升级为“流程顺滑、结果可信、表达清晰”。

## 成功度量

智能化升级是否有效，不看 agent 数量，而看以下指标是否持续改善：

- 用户需要手动做出的决策是否减少。
- 用户需要调整的参数次数是否下降。
- 首轮修复后的剩余问题数是否下降。
- 回滚率是否下降。
- 从选择文件到拿到可行动结果的总时长是否缩短。
- 用户对异常原因和修复效果的理解是否增强。

Agent 数量不是 KPI，用户负担、修复质量、解释能力和系统可靠性才是 KPI。

## 非目标与边界

以下方向不属于本项目当前智能化升级的目标：

- 仅为了显得更 AI 化而替换已经有效的 `LightGBM`、规则扫描或 Gower 能力。
- 让多个 agent 重复实现同一套扫描逻辑。
- 在缺乏验证和回滚保护的情况下直接进行不可控自动写入。
- 过早将架构绑定到单一 LLM 供应商。
- 用不可解释、不可复盘的黑盒行为换取表面上的“自动化感”。

## 文档使用方式

后续涉及下列主题的设计与实现，应优先回到本文件校验方向是否一致：

- multi-agent 架构设计
- Tool Layer 设计
- 自动化修复策略
- 验证与回滚机制
- 前端体验升级
- 图表与结果表达体系

如果某项实现虽然短期可行，但与本文档定义的长期方向冲突，应优先修正设计，而不是修改纲领来迁就短期实现。
